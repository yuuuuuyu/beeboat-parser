import { BTPViewContext } from 'beeboat-plus'

export default class IndexViewContext extends BTPViewContext {
    // 重写 buildView 方法
    // 实现自定义的 view 构建逻辑
    buildView(data: any): void {
        super.buildView(data)
    }
}
